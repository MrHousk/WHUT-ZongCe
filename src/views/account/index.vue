<template>
  <div class="account-container">
    <div class="account">
      <el-menu :default-active="activeTab" mode="horizontal" router>
        <el-menu-item index="profile">个人信息管理</el-menu-item>
        <el-menu-item v-if="$store.getters.account == '1101'" index="account-manage">用户信息管理</el-menu-item>
      </el-menu>

      <div class="main">
        <transition name="fade" mode="out-in">
          <keep-alive>
            <router-view :key="$route.path.replace(/\//g, '_')"></router-view>
          </keep-alive>
        </transition>
      </div>
    </div>
  </div>
</template>

<script>
  export default {
    computed: {
      activeTab: function () {
        return this.$route.path.substring(this.$route.path.lastIndexOf('/') + 1);
      }
    }
  }
</script>

<style lang="scss" scoped>
  @import "~@/styles/vars.scss";
  .account-container {
    width: 100%;
    height: 100%;
    overflow: auto;
    box-sizing: border-box;
    padding: 20px;
    display: flex;
    flex-direction: column;
    .account {
			flex: 1 0;
      width: 800px;
      box-sizing: border-box;
      margin: auto;
      background-color: $color-white;
      border: 1px solid $color-greyLine;
      .main {
        height: calc(100% - 61px);
        box-sizing: border-box;
        padding: 15px 25px;
        overflow: auto;
      }
    }
  }
</style>
